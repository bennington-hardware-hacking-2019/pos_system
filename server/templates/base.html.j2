<!DOCTYPE html>
<html lang="en" dir="ltr">
	<head>
		<meta charset="utf-8">
		<meta name="viewport" content="width=device-width, initial-scale=1">
		<title>TAPA - {% block title %}Thrift and Public Apparel{% endblock %}</title>
		<link rel="stylesheet" href="{{ url_for('static', filename='tapa.css') }}">
		{%- if socket|default(False) %}
		<script src="//cdnjs.cloudflare.com/ajax/libs/socket.io/1.3.6/socket.io.min.js"></script>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
		{%- endif %}
		{%- block head %}
		{%- endblock %}
	</head>
	<body class="{{ size|default('-m') }}">
		<header>
			{%- block header %}
				<img class="logo" src="{{ url_for('static', filename='images/logo{0}.png'.format(size|default('-m'))) }}" alt="TAPA Logo" title="TAPA — Thrift and Public Apparel">
			{%- endblock %}
		</header>
		<main>
			{%- block main %}
			<section><h1>Welcome to TAPA</h1></section>
			{%- endblock %}
		</main>
		<footer>
			{%- macro button(name, url=name, title=name|capitalize, color="black") %}
			<a href="{{url}}" title="{{title}}" class="{{color}}">
				<section>
					<img src="{{ url_for('static', filename='images/{0}.png'.format(name)) }}" alt="{{title}} icon">
					<h3 class="ui">{{title}}</h3>
				</section>
			</a>
			{%- endmacro %}
			{%- block footer %}
				{{- button("home", url="/") }}
			{%- endblock %}
		</footer>
		{% block script %}
		{% endblock %}
	</body>
</html>
